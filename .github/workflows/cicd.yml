name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        run: docker login -u Kene38 -p Ihifix@Docker281

      - name: Build the Docker image
        # run: docker build -t kene38/shipmate .
        run: docker build -t kene38/shipmate -f ./Dockerfile .

  
      - name: Push your Docker Image
        run: docker push kene38/shipmate

